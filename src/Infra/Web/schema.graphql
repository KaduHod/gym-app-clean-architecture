type User {
    id: ID!
    name: String!
    nickname: String!
    email : String!
    password : String!
    cellphone: String
}

type Aluno {
    id: ID!
    user: User
}

type Query {
    users : [User]
    user(id: ID!):User 
    alunos : [Aluno]
}
